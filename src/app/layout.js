import './globals.css';

export const metadata = {
  title: 'Ramiz — Software Engineer Portfolio',
  description: 'Crafting innovative digital experiences through clean code and creative engineering.',
};

export default function RootLayout({ children }) {
  return children;
}
